import { Routes } from '@angular/router';
import { SpaceAllocatorComponent } from 'src/space-allocator/space-allocator.component';

export const routes: Routes = [
    {
        path: 'allocate-space',
        component: SpaceAllocatorComponent
    }
];